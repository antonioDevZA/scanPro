# scanPro

A minimal document scanner that scans a directory and lists supported document/image files.

## Usage

```bash
python scanner.py /path/to/documents
```

The command prints JSON entries with each file's relative path and size in bytes.